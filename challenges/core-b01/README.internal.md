# core-b01 internal

The public workflow begins at the inventory search page and exposes a normal query first.
The intentionally vulnerable query must be confirmed by comparing normal and malformed results.
The protected configuration row is stored server-side and contains the per-instance flag.
